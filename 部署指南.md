# 增重打卡 - 部署指南（小白版）

## 你需要准备什么
- 一个 GitHub 账号（去 https://github.com 用邮箱注册，免费，5分钟）

---

## 步骤一：在 GitHub 上创建仓库

1. 登录 GitHub 后，点右上角 **+** → **New repository**
2. Repository name 填：`zengzhong` （或你喜欢的名字）
3. 选 **Public**（公开）
4. **不要勾选** "Add a README file"
5. 点绿色 **Create repository** 按钮

做完后你会看到一个页面，标题是 "Quick setup"，下面有一堆命令。

---

## 步骤二：打开终端上传文件

Mac 上按 `Cmd+空格`，输入 `Terminal`，回车打开终端。

**一行一行复制粘贴下面的命令**（每行粘贴后按回车）：

```bash
# 1. 进入文件夹
cd "/Users/aixi/WorkBuddy/2026-05-30-22-38-59/增重计划"

# 2. 初始化 Git（如果提示已存在，忽略继续）
git init

# 3. 添加所有文件
git add training-tracker.html manifest.json sw.js icon-192x192.png icon-512x512.png icon.svg

# 4. 提交
git commit -m "增重打卡 PWA v1"

# 5. 连接到你的仓库（把下面的 YOUR_USERNAME 换成你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/zengzhong.git

# 6. 改分支名
git branch -M main

# 7. 推送
git push -u origin main
```

推送时会弹窗让你登录 GitHub，选择 "Sign in with your browser" 即可。

---

## 步骤三：开启 GitHub Pages

1. 回到 GitHub 上的仓库页面
2. 点顶部 **Settings** 标签
3. 左边菜单找 **Pages**（在 "Code and automation" 下面）
4. "Branch" 下面选 **main**，旁边选 **/ (root)**，点 **Save**
5. 等 1-2 分钟，刷新页面，你会看到一个绿色的框：
   > Your site is live at `https://YOUR_USERNAME.github.io/zengzhong/`

---

## 步骤四：发给朋友

把那个链接发给子豫。

### 他用 iPhone 打开后的操作：
1. Safari 打开链接
2. 点底部中间的 **分享** 按钮（方框带箭头）
3. 往下滑，点 **添加到主屏幕**
4. 点右上角 **添加**

完成后主屏幕上会有一个"增重打卡"的图标，点开就像独立 App，没有浏览器栏。

---

## 以后你改了代码怎么更新？

```bash
cd "/Users/aixi/WorkBuddy/2026-05-30-22-38-59/增重计划"
git add training-tracker.html
git commit -m "更新xxx功能"
git push
```

推送后 1-2 分钟自动生效。朋友那边刷新页面就能看到更新。
