# 增重打卡 App — 产品交接文档

> 整理时间：2026-05-31
> 目标读者：接手开发的工程师

---

## 一、项目背景

为一名在澳洲生活的用户（男，24岁，176cm，50kg，BMI 16.1，偏瘦体型）制作一款手机端增重训练打卡工具。

训练方案参考**卓叔增重教学体系**，分两个阶段：

- **Phase 1（第 1-6 周）**：能量系统修复，目标体重 50 → 52-53 kg
  - 动作：3 个全身性复合动作
  - 强度：10-15 RM（渐进超负荷）
  - 频率：每周 3 次（一三五或二四六）
- **Phase 2（第 7-12 周）**：渐进增肌，目标体重 53 → 55-57 kg
  - 强度过渡到 8-12 RM
  - 组数增加、动作可扩展

**蛋白粉**：Famous Nutrition Protein + Creatine Powder（乳清蛋白 + 肌酸复合粉，非增重粉 Mass Gainer，安全可用）

---

## 二、当前实现形式

**单文件 HTML**（`training-tracker.html`），无后端，所有数据存储于 `localStorage`。

可直接在手机浏览器打开，支持通过 iOS Safari「添加到主屏幕」实现类 App 体验。

### 技术栈

- **结构**：纯 HTML + 内嵌 CSS + 内嵌 JavaScript，单文件，无依赖
- **字体**：Google Fonts CDN 引入 `Noto Serif SC`（400/600 权重）
- **数据持久化**：`localStorage`，键值规则如下：
  - `zx_YYYY-MM-DD`：训练打卡数据（JSON）
  - `wt_YYYY-MM-DD`：体重记录（数字字符串）
  - `q_YYYY-MM-DD`：当日问答状态（`{ work: boolean|null, train: boolean|null }`）
  - `theme_dark`：主题偏好（`"true"` / `"false"`）
  - `last_{exerciseWeightKey}`：各动作上次训练重量（用于跨日继承）

---

## 三、已实现功能

| 功能 | 状态 | 备注 |
|------|------|------|
| 深色/浅色主题切换 | ✅ 完成 | CSS 变量 + `data-theme` 属性 |
| 今日情况问答（上班日/休息日 + 训练/休息） | ✅ 完成 | 问答结果驱动饮食推荐和训练计划渲染 |
| 饮食推荐（按日期哈希轮换，可展开） | ✅ 完成 | 见 §五 待优化事项 |
| 训练动作打卡（Accordion 卡片） | ✅ 完成 | 展开/收起，逐组打卡 |
| 重量 / 次数输入 | ✅ 完成 | 跨日继承上次训练重量 |
| 渐进超负荷提示 | ✅ 完成 | 最后一组超过目标次数时显示加重建议 |
| 打卡完成后跳转「今日已打卡」卡片 | ⚠️ 已修复但待验证 | 见 §六 已知 Bug |
| 完成卡片显示随机鼓励语「子豫，…」 | ✅ 完成 | 随机从 `ENCOURAGEMENTS` 数组取 |
| 修改打卡记录入口 | ✅ 完成 | 已打卡后显示「去修改打卡记录」按钮 |
| 体重记录 + 14 天趋势图（柱状） | ✅ 完成 | 独立「体重」Tab |
| 打卡历史记录 | ✅ 完成 | 独立「记录」Tab |
| 概览卡片（当前体重/目标进度/本周变化/完成率） | ✅ 完成 | 顶部 2×2 Grid |
| Confetti 庆祝动画 | ✅ 完成 | 打卡完成时触发 |
| 统计弹窗 | ✅ 完成 | 底栏「统计」按钮 |
| 日期导航（前后切换 + 回到今天） | ✅ 完成 | |

---

## 四、UI 设计规范

### 色彩（CSS 变量）

```css
/* Light Theme */
--bg: #f5f5f7;
--card: #ffffff;
--border: #e8e8ed;
--text: #1d1d1f;
--text2: #6e6e73;
--text3: #aeaeb2;
--accent: #0071e3;      /* 主色调，蓝 */
--green: #34c759;       /* 完成/正向 */
--orange: #ff9f0a;      /* 提示/卡路里 */
--red: #ff3b30;         /* 警告/休息日 */

/* Dark Theme */
--bg: #1a1a1c;
--card: #242426;
--accent: #d4a574;      /* 暖棕金，区别于 light 的蓝 */
```

### 字体

当前方案（待最终确认）：全页使用 Noto Serif SC + 系统衬线降级。

**工程师注意**：用户对字体方案仍有调整意向，可能改回「标题衬线 + 正文无衬线」搭配，具体见 §五。

```css
--font: "Noto Serif SC", "Source Han Serif SC", "Songti SC", serif;
--font-display: "Noto Serif SC", "Source Han Serif SC", "Songti SC", serif;
```

### 布局

- 最大宽度：无限制，移动端优先
- 内边距：`16px` 横向
- 底部 Padding：`120px`（为 fixed 底栏留空间）
- 安全区：`env(safe-area-inset-bottom)` 适配 iPhone 刘海/底条

---

## 五、待优化事项（产品层面）

### 5.1 字体方案（未定稿）

用户反馈：「全是衬线容易审美疲劳，希望衬线与另一种字体搭配」。

**建议工程师实现的方案**：
- 标题（h1、section-label）：Noto Serif SC 600
- 正文（按钮、卡片内容、小标签）：PingFang SC / SF Pro（系统无衬线）
- 数字（体重数值、卡路里数值）：SF Mono 或 tabular-nums

### 5.2 饮食建议（内容层面，未定稿）

用户反馈：现有方案「太西式（白人饭）」，需要调整为更符合在澳洲华人日常的食材。

**用户诉求**：
- 减少：三明治、烤豆、Weet-Bix、Acai Bowl 等纯西式食物
- 保留/增加：米饭、盖饭、炒饭、方便面、速冻饺子、鸡蛋、牛奶、澳洲超市可购中式食材
- 风格：实用、简洁、可操作性强，不需要太详细的菜谱

**当前代码位置**：JS 里的 `WORKDAY_PLANS` 和 `RESTDAY_PLANS` 数组（约第 1015-1093 行）

### 5.3 训练计划（待产品确认）

当前 HTML 里的训练动作：
1. 高脚杯深蹲（下肢，5组×10-15次）
2. 哑铃卧推（胸，4组×10-15次）
3. 单臂哑铃划船（背，4组×10-15次）
4. 平板支撑（核心，3组×30-60秒）

**待确认问题**：
- 该方案偏重上肢，缺少肩部（哑铃推举）动作
- 是否符合卓叔推荐的 Phase 1 全身训练方案？
- 动作是否需要根据用户实际器材情况调整？

**代码位置**：JS 里的 `EXERCISES` 数组（约第 759-763 行）

---

## 六、已知 Bug

### Bug 1：打卡完成后不跳转「今日已打卡」页面（核心 Bug）

**现象**：完成所有动作后点击「完成打卡」按钮，训练动作卡片不消失，不显示「今日已打卡」完成卡片。

**根因分析**：

1. `saveDay()` 需要所有动作的组数全部完成（`allComplete === true`）才能执行打卡逻辑。但在测试中，`allComplete` 判断可能失败。

2. **JS 变量名拼写错误**（历史遗留，已修复）：
   - 错误：`upgrades.length > 0`（`upgrades` 多一个 s）
   - 正确：`upgrades.length > 0`
   - 位置：`saveDay()` 函数内，约第 1368 行

3. `saveDay()` 中调用 `render()` 的时机问题：
   - `render()` 需要在 `currentData.completed = true` 且 `saveData()` 之后立刻调用
   - 当前代码在 `celebrate()` 之后调用 `render()`，逻辑上正确，但建议验证

**建议验证步骤**：
1. 手动将 localStorage 中当日数据的 `completed` 设为 `true`，刷新页面，看是否显示完成卡片
2. 实际操作打完所有动作的所有组，点击完成，看 `currentData.completed` 是否正确变为 `true`

**相关代码**：
```javascript
// saveDay() 函数关键逻辑（约 1342-1379 行）
function saveDay() {
  if (qTrain === null) { showToast('请先回答上方两个问题 😊'); return; }
  if (!qTrain) { showToast('休息日好好吃饭休息 😴'); return; }
  
  let allComplete = true;
  EXERCISES.forEach(ex=>{
    if (currentData.exercises[ex.id].completedSets.length < ex.setsTarget) allComplete = false;
  });
  if (!allComplete) { showToast('还有动作没做完，继续加油！'); return; }  // ← 卡在这里
  
  currentData.completed = true;
  saveData(currentDate, currentData);
  celebrate();
  render();  // ← 这里会重新渲染显示完成卡片
  ...
}
```

```javascript
// renderStatusAndTraining() 完成卡片逻辑（约 1213-1225 行）
if (currentData && currentData.completed) {
  const enc = ENCOURAGEMENTS[Math.floor(Math.random()*ENCOURAGEMENTS.length)];
  mainArea.innerHTML = `
    <div class="done-card">
      <div class="done-icon">✅</div>
      <div class="done-title">今日已打卡</div>
      <div class="done-enc" ...>子豫，${enc}</div>
      <div class="done-sub">辛苦了，好好休息 💪</div>
      <button class="secondary-btn" onclick="resetToday()">✏️ 去修改打卡记录</button>
    </div>`;
  return;
}
```

---

## 七、文件路径

```
增重计划/
├── training-tracker.html          # 主文件（当前版本）
└── 3个月皮包骨增重计划.md          # 完整训练计划文档（卓叔体系）
```

---

## 八、后续方向（供参考）

用户有意将这个工具做成正式小程序。可能的路径：

1. **微信小程序**（推荐）：将当前 HTML 逻辑迁移至 WXML/WXSS/JS，数据可存云开发或本地 `wx.setStorage`
2. **PWA 增强**：在当前 HTML 基础上添加 `manifest.json` + `service-worker.js`，增强离线体验

---

*文档由 AI 辅助整理，如有问题请联系 aixi*
